import AllocationCockpit from '@/components/AllocationCockpit';

export default function CockpitPage() {
    return (
        <main style={{ height: '100vh', width: '100vw', overflow: 'hidden' }}>
            <AllocationCockpit />
        </main>
    );
}
